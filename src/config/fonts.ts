/**
 * Configurações de fontes da aplicação
 */
export const fontConfig = {
	sans: {
		variable: '--font-geist-sans',
		subsets: ['latin'],
	},
	mono: {
		variable: '--font-geist-mono',
		subsets: ['latin'],
	},
} as const;
